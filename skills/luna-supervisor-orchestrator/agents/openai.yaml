interface:
  display_name: "Luna Supervisor Orchestrator"
  short_description: "Supervisor-owned Luna DAG orchestration"
  default_prompt: "Use $luna-supervisor-orchestrator to coordinate bounded GPT-5.6 Luna work with proportional writers, phase barriers, and integration or review when applicable."
